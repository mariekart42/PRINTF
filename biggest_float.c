#include "ft_printf.h"
#include<float.h>
int main()
{
     // float set = 3.4028237;

     // int exponent = 1;

     // printf("float: %f\n", set);
     
     // while (exponent <= 38)
     // {
     //      set *= exponent;
     //      printf("set_while: %f\n", set);
     //      exponent+=1;
     // }
     // printf("after: %f\n", set);

     printf("float_max: %f\n", FLT_MAX);
     printf("float_min: %f\n", FLT_MIN);
}